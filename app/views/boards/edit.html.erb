<div class="container">
    <h2>掲示板</h2>
    <div class="content">
<h2><%= link_to @board.title, board_path(@board) %></h2>

<%= form_for @board do |f| %>
<p>タイトル</p>
<%= f.text_field :title %>
<p>作成者名</p>
<%= f.text_field :editor %>
<%= f.submit %>

<% end %>
</div>