# Use the README file as a reference to customize settings.

Comment:
  fields:
    default: user_id, broker_id, body, title, support, design, feeling, payment
    form: user_id, broker_id, body, title, support, design, feeling, payment
  relationships: 
  application: Application
